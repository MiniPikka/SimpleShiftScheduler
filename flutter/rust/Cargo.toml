[package]
name = "shift-flutter-bridge"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
shift-algorithm = { path = "../../shift-core/crates/shift-algorithm" }
shift-statistics = { path = "../../shift-core/crates/shift-statistics" }
leave-optimizer = { path = "../../shift-core/crates/leave-optimizer" }
holiday-engine = { path = "../../shift-core/crates/holiday-engine" }
shift-export = { path = "../../shift-core/crates/shift-export" }
chrono = "0.4"
serde_json = "1.0"
